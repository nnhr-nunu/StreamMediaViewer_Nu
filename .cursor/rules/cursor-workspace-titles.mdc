---
description: Cursor Workspaces 作業リスト／チャットタブ名を日本語形式に統一
alwaysApply: true
---

# Workspaces 作業リストタイトル

```
【対象画面】依頼概要
```

- ユーザー意図が読めた着手直後に MCP `rename_chat` を 1 回呼ぶ
- 対象画面は手元 / 配信出力 / 安全ゲート / メディア一覧 など。複数なら `/` で 2 つまで
- 依頼概要は 20 字以内の日本語
- 英語のみ・ファイルパスそのまま・汎用名（`New chat`）は禁止
